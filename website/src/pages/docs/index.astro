---
import { getCollection } from 'astro:content';
import DocsLayout from '../../layouts/DocsLayout.astro';

const docs = (await getCollection('docs')).sort(
  (left, right) => left.data.order - right.data.order,
);
---

<DocsLayout title="Docs — Mittals AI">
  <header class="docs-header">
    <p class="section__eyebrow">Docs</p>
    <h1>Start with the right Mittal.</h1>
    <p>Install the family once, then pick the guide for Claw, Coder, or Recipes.</p>
  </header>

  <ul class="docs-index-list">
    {docs.map((entry) => (
      <li>
        <a href={`/docs/${entry.slug}/`}>
          <strong>{entry.data.title}</strong>
          {entry.data.description && <span>{entry.data.description}</span>}
        </a>
      </li>
    ))}
  </ul>
</DocsLayout>
